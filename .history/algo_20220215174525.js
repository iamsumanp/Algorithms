// ? const sameArrayO = (arr) => {
//  ? for (let i = 0; i < arr.length; i++) {
//   ?  for (let j = 0; j < arr.length; j++) {
//   ?    console.log(arr[i]);
//   ?  }
//  ?  }
// ? };

// ? sameArrayO(["abc", "def", "ghi"]);

const aarr = [0, 1, 2, 4, 7];
// console.log(aarr.length - 1);

const aarrSize = aarr.length;
mergeSort(aarr, 0, aarrSize - 1);
